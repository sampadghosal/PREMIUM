name: Run Silent Premium Bot

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

concurrency:
  group: "silent-premium-bot"
  cancel-in-progress: true

jobs:
  bot:
    runs-on: ubuntu-latest
    timeout-minutes: 330

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Bot
        env:
          # Telegram
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          ADMIN_ID: ${{ secrets.ADMIN_ID }}

          # Firebase
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_CLIENT_EMAIL: ${{ secrets.FIREBASE_CLIENT_EMAIL }}
          FIREBASE_PRIVATE_KEY: ${{ secrets.FIREBASE_PRIVATE_KEY }}
          FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
          KEY_ENCRYPTION_KEY: ${{ secrets.KEY_ENCRYPTION_KEY }}

          # VPLINK / Free Access
          VPLINK_API_TOKEN: ${{ secrets.VPLINK_API_TOKEN }}
          VPLINK_API_URL: ${{ secrets.VPLINK_API_URL }}
          FREE_VERIFY_URL: ${{ secrets.FREE_VERIFY_URL }}
          FREE_CLAIM_TTL_SECONDS: ${{ secrets.FREE_CLAIM_TTL_SECONDS }}

        run: |
          python -u bot.py